# This PC inventory template

Fill after running `..\scripts\run-windows-inventory.ps1` and `..\scripts\export-winget.ps1`.

| Field | Value |
|-------|--------|
| Date (ISO-8601) | |
| OS (from CIM) | |
| WSL distros (`wsl -l -v`) | |
| Primary workspace path | `G:\workspaces\Kinoite` |

**Attachments:** point to sanitized files under `imports/` (do not commit secrets).
