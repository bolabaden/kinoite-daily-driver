# WSLg: clipboard and GUI

- **Clipboard** is usually shared between Windows and WSLg automatically.
- **HiDPI:** if UI is blurry, adjust **Windows display scaling** and WSLg settings as per current Microsoft docs.
- **Wayland** vs **X11** fallback depends on the Linux app; Plasma under WSLg may need experimentation.
