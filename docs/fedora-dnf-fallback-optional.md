# Optional: classic Fedora (dnf) in WSL — **not** Phase A

Use **only** when you **explicitly** accept that this does **not** replace Kinoite/OSTree Phase A goals (e.g. a one-off container host, or Kinoite-in-WSL2 is blocked).

- Microsoft Store **Fedora** / `wsl --install` dnf images: fine for **general Linux** work.
- Label any docs or automation so it cannot be confused with **`kinoite-wsl2.md`** Phase A.

No script here is **default**; see `../scripts/fedora-dnf-fallback.sh` stub.
