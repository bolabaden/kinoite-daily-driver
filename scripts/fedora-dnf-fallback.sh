#!/usr/bin/env bash
echo "DEPRECATED for Phase A: classic Fedora (dnf) in WSL is NOT a substitute for Kinoite/OSTree goals."
echo "Use only with explicit approval. See ../docs/fedora-dnf-fallback-optional.md"
exit 1
